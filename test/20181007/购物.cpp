#include <iostream>
#include <cstdio>
#include <algorithm>
#include <queue>
using namespace std;

priority_queue<int> hehe;
int a[100010];

int main(){
    int N, K;
    scanf("%d%d", &N, &K);
    for (int i=0; i<N; i++) scanf("%d", a[i]);
    sort(a, a+N);
    for (int i=0; i<2*K; i++){
        
    }
}