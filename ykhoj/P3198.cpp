#include <iostream>
#include <cstdio>
using namespace std;

int main(){
    int T, Maxx, Maxy, Ax, Q, N;
    scanf("%d%d%d%d%d%d", &T, &Maxx, &Maxy, &Ax, &Q, &N);
    for (int i=0; i<N; i++){
        scanf("%d%d%d");
    }
}